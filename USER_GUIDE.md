# MindSwitch 使い方ガイド

## はじめに

MindSwitchは、毎朝約3分で今日のマインドをセットするためのアプリです。すべての項目は「参考」であり、無理に前向きになる必要はありません。答えたくない項目はスキップできます。

## ホーム画面への追加

1. スマホのブラウザでMindSwitchのURLを開きます。
2. **iPhone（Safari）**：共有ボタン →「ホーム画面に追加」
3. **Android（Chrome）**：メニュー（︙）→「ホーム画面に追加」または「アプリをインストール」

追加後はアプリのようにアイコンから起動でき、オフラインでも使えます。

## 4つのステップ

1. **ご機嫌約束**：今日の気分を選びます。
2. **ネガティブ変換**：不安や心配ごとを入力すると、見方を変えるヒントが表示されます（スキップ可）。
3. **ベビーステップ**：やりたいことを、今すぐ始められる小さな一歩に分解します。
4. **集中宣言**：一番集中したいことと、最初の行動を決めます。

入力中に閉じても下書きが保存され、次回開いたときに続きから再開できます。

## その日の内容の確認・修正

ホーム画面から今日の記録を確認できます。内容を直したいときは「修正」から該当ステップを編集できます。

## Obsidianへのコピー

完了画面・履歴の詳細画面から、その日の内容をObsidian等のメモアプリに貼り付けやすい形式でコピーできます。

## 履歴・検索・カレンダー

- 履歴画面では過去の記録を一覧・検索できます。
- カレンダー表示で、達成した日を確認できます。

## ゴミ箱

削除した記録はゴミ箱に移動し、元に戻すか完全に削除するか選べます。

## バックアップ・JSON書き出し／読み込み

設定画面から、データのバックアップ作成・JSON形式での書き出し（エクスポート）・読み込み（インポート）ができます。機種変更時や万一のトラブル時にご利用ください。

## 設定

- テーマ（ライト／ダーク／端末に合わせる）
- 文字サイズ
- 通知（毎朝の時刻に通知）
- サウンド・バイブレーション

## データについて

入力内容は、この端末の中だけに保存されます。外部のサーバーへは送信されません。

## アプリの更新について

新しいバージョンが公開されると、画面下に「新しいバージョンがあります」という通知が表示されます。タップすると最新版に切り替わります。
